/**
 * Project orchestrator — public barrel.
 *
 * Consumers import from `@/agents/project-orchestrator` (no path change
 * required by Phase 5d split — the original file became this directory).
 */

export { runProjectOrchestrator } from './runner';
